{
  "name": "@razorrsd/ytnotify",
  "version": "1.0.11",
  "description": "",
  "main": "build/src/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist",
    "test": "jest --config jestconfig.json",
    "release": "standard-version && git push --follow-tags && npm run build && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/RazorRSD/ytnotify.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.3",
    "rimraf": "^5.0.5",
    "standard-version": "^9.5.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@types/events": "^3.0.3",
    "@types/express": "^4.17.21",
    "@types/xml2js": "^0.4.14",
    "axios": "^1.6.7",
    "express": "^4.18.2",
    "xml2js": "^0.6.2"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
